extends ../layout

block content
  img.src(src=`${user.photos[0].value}`, alt="")
  p ID #{user.id}
  p Name #{user.displayName}
  p City #{user._json.city}

  each activity in activities
    a(href=`activity/${activity.id}`)
      - var thumb = activity.map.summary_polyline ? "✅" : "🔴"
      p= `${activity.name} ${thumb}`